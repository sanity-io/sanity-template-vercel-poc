{
  "private": true,
  "name": "sanity-zeit-poc",
  "version": "1.0.1",
  "scripts": {
    "build-studio": "(cd studio && npm run build)",
    "dev": "npm run build-studio && now dev",
    "format": "lerna run format",
    "lint": "lerna run lint",
    "postinstall": "lerna bootstrap"
  },
  "devDependencies": {
    "now": "^16.5.2",
    "@sanity/cli": "^0.144.0",
    "lerna": "^3.18.4"
  }
}
