{
  "private": true,
  "name": "sanity-zeit-poc",
  "version": "1.0.1",
  "scripts": {
    "build": "rimraf public && lerna run build --parallel && cp -R web/public public && cp -R studio/public public/studio",
    "dev": "echo 'TODO make this better'",
    "format": "lerna run format",
    "lint": "lerna run lint",
    "postinstall": "lerna bootstrap"
  },
  "devDependencies": {
    "now": "^16.5.2",
    "@sanity/cli": "^0.144.0",
    "lerna": "^3.18.4"
  }
}
